server:
  error:
    include-message: always
spring:
  datasource:
    password: 1234
    url: jdbc:postgresql://localhost:5432/crud3
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true